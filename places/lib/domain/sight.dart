// Sight - модель данных
class Sight {
  String name;
  double lat; // широта
  double lon; // долгота
  String url;
  String detail;
  String type;

  Sight(this.name, this.lat, this.lon, this.url, this.detail, this.type);
}
